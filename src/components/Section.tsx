import styled from '@emotion/styled';

const Section = styled.section<any, { [key: string]: any }>`
  padding: 30px 0;
  display: flex;
  flex-direction: column;
  align-items: center;
`;

export default Section;
